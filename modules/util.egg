dict(
  "sum", define(sum, fun(arr, do(
    define(i, 0),
    define(result, 0),

      while(<(i, length(arr)), do(
        set(result, +(result, get(arr, i))),
        set(i, +(i, 1))
      )),

      set(result, result)
  ))),

  "avg", define(avg, fun(arr, do(
    /(sum(arr), length(arr))
  )))
)
