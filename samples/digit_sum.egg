# Sums all digits from 0 to 10

do(
  :=(total, 0),
  :=(count, 1),

  while(<(count, 11), do( # Run 10 times
    =(total, +(total, count)), # Accumulate
    =(count, +(count, 1)))), # Increase counter

  print(total))
