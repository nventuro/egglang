# Sums all digits from 0 to 10

do(
  define(total, 0),
  define(count, 1),

  while(<(count, 11), do( # Run 10 times
    define(total, +(total, count)), # Accumulate
    define(count, +(count, 1)))), # Increase counter

  print(total))
