# Calculates the 10th power of 2

do(
  :=(pow, fun(base, exp, # Recursive power implementation
    if(==(exp, 0),
      1, # Base case
      *(base, pow(base, -(exp, 1)))))), # Recursive call

  print(pow(2, 10)))
